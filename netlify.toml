[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send every path to index.html so client-side routes
# (/weekly, /monthly) survive a direct visit or a refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
